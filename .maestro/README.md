# E2E（Maestro）

**Home のスワイプと読了フローのみ**を対象にする。
画面を網羅せず、中核導線が壊れていないことだけを見る。

```bash
maestro test .maestro/read-flow.yaml
```

シミュレータまたは実機に dev build をインストールしてから実行する。
CI では回していない（macOS ランナーが必要なため）。

**注意**: このフローは一度も実行できていない（Linux 環境で開発しているため）。
最初の実機作業で、セレクタが実際の UI と合っているかを確認して調整すること。
