import { AppRegistry } from 'react-native';

import ShareExtension from './share-extension';

// 第 1 引数は "shareExtension" 固定（expo-share-extension の規約）
AppRegistry.registerComponent('shareExtension', () => ShareExtension);
