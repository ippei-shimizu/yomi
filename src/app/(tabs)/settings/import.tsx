import { Text, View } from 'react-native';

export default function ImportScreen() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text>URL をまとめて追加</Text>
    </View>
  );
}
